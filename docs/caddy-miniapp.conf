# Caddy config block for the Mini App frontend.
# Add this to /etc/caddy/Caddyfile on the production server, then run: caddy reload

expense-sync-bot-app.invntrm.ru {
    root * /var/www/ExpenseSyncBot/miniapp/dist
    file_server
    try_files {path} /index.html
}
