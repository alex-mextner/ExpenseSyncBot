// Dashboard tab — analytics dashboard with configurable widgets

interface Props { groupId: number; }
export function Dashboard({ groupId: _groupId }: Props) {
  return <div>Dashboard (coming soon)</div>;
}
